const nome = 'Menine'// String -> Texto
const nome1 = "Menine"// String -> Texto
const nome2 = `Menine`// String -> Texto

const num = 10;// Number -> Numero
const num2 = 10.52;// Number -> Numero

let nomeAluno; // undefined -> não aponta pra local nenhum na memória. No caso de undefined, é quando não definimos nada e deixamos o js colocar o valor como undefined
let sobrenomeAluno = null // null -> não aponta pra local nenhum na memória. No caso de definirmos, definimos para null quando queremos explicitamente que não aponte para nada

const aprovado = true // Boolean -> True ou False

console.log(typeof nome)
console.log(typeof num)
console.log(typeof nomeAluno)
console.log(typeof sobrenomeAluno)
console.log(typeof aprovado)